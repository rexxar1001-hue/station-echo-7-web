# Медиа-ассеты: какие файлы и под какими именами подставлять

Сейчас все файлы в `img/` и `audio/` — **заглушки**, созданные скриптом
`tools/make_placeholders.py`. Заменяйте их своими файлами, **сохраняя имена и расширения**.
Ничего в коде менять не нужно.

Если нужно пересоздать заглушки: `python3 tools/make_placeholders.py`.

---

## 1. Фотоархив (квест 2) — папка `img/`

| Имя файла | Что это | Требования |
|---|---|---|
| `field_photo_087.jpg` | **ВАЖНЫЙ файл.** Фото Viktor Orel — лесистый берег, без видимых подсказок | В JPG должны быть вшиты EXIF-теги GPS (см. ниже) |
| `field_photo_005.jpg`, `_008`, `_012`, `_019`, `_023`, `_031`, `_038`, `_044`, `_052`, `_061`, `_066`, `_075`, `_084`, `_097`, `_103`, `_111`, `_128`, `_140`, `_149` | Фон архива, 19 любых фото | EXIF не нужен (лучше, чтобы GPS в них не было) |

Список имён задан в `photos.html` (массив `PHOTO_IDS`) и в `tools/make_placeholders.py`.

### EXIF, обязательный для `field_photo_087.jpg`

| Тег | Значение |
|---|---|
| GPSLatitude | 45° 53' 32.28" N |
| GPSLongitude | 13° 03' 51.12" E |
| GPSDestLatitude | 45° 54' 08.40" N (ложный след) |
| GPSDestLongitude | 13° 01' 12.00" E (ложный след) |

Проще всего вшить их так: положить своё фото в `img/field_photo_087.jpg` и запустить

```bash
python3 tools/embed_exif.py img/field_photo_087.jpg
```

(скрипт перезапишет EXIF нужными значениями, картинку не трогает).

---

## 2. Карта острова (квест 3) — `img/island.png`

* Размер полотна: **1500 × 1000** пикселей (можно другое соотношение, но тогда
  поправьте `IMG_W` / `IMG_H` в `map.html`).
* На карте не должно быть подписей точек — точки рисует Leaflet поверх картинки.
* Положение точек задаётся в пикселях: `AP-03 (610, 420)`, `AP-07 (640, 455)`,
  `AP-11 (655, 470)` — координаты в `js/config.js` (`mapPoints`), остальные
  12 фоновых точек — в `map.html` (`OTHER_POINTS`). Счёт координат: X слева направо,
  Y снизу вверх (нижний левый угол = 0,0).

---

## 3. Радиоархив (квест 4) — папка `audio/AP-07/`

14 файлов, верный — `rec_142.7.wav`:

```
rec_38.6.wav   rec_47.2.wav   rec_59.4.wav   rec_71.9.wav
rec_88.3.wav   rec_96.5.wav   rec_104.8.wav  rec_117.2.wav
rec_128.0.wav  rec_142.7.wav  ← ВЕРНЫЙ      rec_166.4.wav
rec_181.3.wav  rec_205.1.wav  rec_219.6.wav
```

* Формат: WAV (PCM). Длительность 3–15 секунд.
* В `rec_142.7.wav` фраза про сектор GAMMA должна быть записана **задом наперёд**,
  чтобы кнопка Reverse на сайте её «выпрямила». То есть: запишите нормальную фразу,
  разверните её любым редактором (или командой ниже) и сохраните развёрнутую версию.

```bash
ffmpeg -i phrase.wav -af areverse audio/AP-07/rec_142.7.wav
```

* Остальные 13 файлов — шум/помехи/обрывки без смысла (в том числе в реверсе).

### Спектрограммы (картинки, не реальный анализ) — папка `img/`

| Имя файла | Что на картинке |
|---|---|
| `spectrogram_rec_142.7.png` | **ВАЖНАЯ.** Спектрограмма с вплетённой надписью `SHADOW-01` |
| `spectrogram_rec_<частота>.png` для остальных 13 файлов | Обычная спектрограмма без надписей |
| `spectrogram_noise.png` | Резервная картинка: показывается, если файла для конкретной записи нет |

Рекомендуемый размер: 800 × 300.

---

## 4. Видеонаблюдение (квест 7) — папка `img/`

| Имя файла | Что это |
|---|---|
| `camera_01_empty.jpg` … `camera_10_empty.jpg` | Пустые кадры 10 камер |
| `camera_04_figure.jpg` | **ВАЖНЫЙ.** Тот же кадр камеры 04, но с силуэтом человека |

Сайт сам подменяет кадр камеры `GAMMA-CAM-04`: каждые ~4 секунды на 400 мс
показывает `camera_04_figure.jpg`. Кадры `empty` и `figure` должны быть сняты
с одной точки, иначе эффект «мелькания» ломается. Размер: 320 × 180 или больше
(одинаковый для всех).

---

## 5. Реплика из громкоговорителя (квест 7) — `audio/speaker_gamma_cam_04.wav`

Короткая (5–10 с) реплика раненого Viktor Orel. Текст, который выводится на экране
рядом с аудио, задан в `cameras.html` (константа `SPEAKER_LINE`) — при изменении
записи поправьте и его.

---

## 6. Логотип вкладки (favicon) — `img/logo.png`

Подключён на всех страницах. Замените файл `img/logo.png` своим — имя и путь менять не нужно.
Рекомендуется квадратный PNG 256 × 256 (подойдёт и 64 × 64). Сейчас там заглушка.

---

## 7. Что ещё заполняется вручную

| Файл | Что вписать |
|---|---|
| `diary-vorel-087.html` | Полный текст дневника. Сейчас там заглушка с **временным кодом 4729** — заменить на реальный код физического замка |
| `js/config.js` | Единая точка настройки: логин/пароль, id сотрудника, координаты, частоты, схема проводов, имя камеры |
